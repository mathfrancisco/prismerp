import { InventoryTransaction } from './inventory-transaction.model';

describe('InventoryTransaction', () => {
  it('should create an instance', () => {
    expect(new InventoryTransaction()).toBeTruthy();
  });
});
